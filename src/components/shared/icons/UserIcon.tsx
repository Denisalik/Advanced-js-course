import React from "react";
import { SvgIcon, SvgIconProps } from "@mui/material";

const UserIcon: React.FC<SvgIconProps> = ({ sx, ...props }) => (
  // Icons from svg(import? copy?)
  <SvgIcon />
);

export default UserIcon;
